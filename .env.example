# PlatformCore environment — template
#
# Copy this file to `.env` (which is gitignored) and fill in real values.
# Compose auto-loads `.env` from the project root for variable substitution
# inside docker-compose.yml. No secret should ever live inside the YAML.

POSTGRES_USER=platformcore
POSTGRES_PASSWORD=changeme
POSTGRES_DB=platformcore
