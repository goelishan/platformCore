apiVersion: v1
kind: Secret
metadata:
  name: nginx-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "platformcore.labels" . | nindent 4 }}
    app.kubernetes.io/component: nginx
type: Opaque
stringData:
  API_KEY: {{ .Values.nginx.secret.apiKey | quote }}